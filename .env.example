# Binance API Credentials
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_secret_key_here

# Email Notifications (Gmail Example)
EMAIL_NOTIFICATIONS_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_FROM_EMAIL=your.email@gmail.com
SMTP_PASSWORD=your_app_password_here
NOTIFICATION_EMAIL=client.email@example.com

# Instructions for Gmail:
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Generate an "App Password" for "Mail"
# 3. Use that 16-character password as SMTP_PASSWORD
# 4. SMTP_FROM_EMAIL = your Gmail address
# 5. NOTIFICATION_EMAIL = where to send trade alerts (can be same or different)
